# 背景

我们是公司内部的团队，完全不懂技术。现在会维护一个notebooklm，这个notebooklm只供公司内部其他团队使用，现在其他团队每次都要打开网页访问notebooklm，另外我们维护的notebooklm会隔一段时间就更换一次。

公司内部同事大部分都在使用很多AI coding工具。比如 Gemini CLI、Claude Code 等，他们不想每次都打开网页，觉得麻烦，而想能够在AI coding工具中直接去向notebooklm提问。

# 需求

基于现在这个开源项目，我们想给内部同事提供一个非常方便的工具。这个工具主要想实现以下几点:
1. 不需要其他人手动执行很多条命令去安装，最好能够直接执行一条命令就能够安装好；或者另一个思路是我们提供一个单独的MCP Server，部署在云端，但云端部署这个思路可能对我们来说太难了，需要仔细考虑可行性。
2. 安装好后，内部同事通过notebooklm鉴权之后，就能在AI coding工具中直接用自然语言就能够向notebooklm提问。
3. 内部同事通过notebooklm鉴权这个步骤，最好也能自动化处理。
4. 按照 NOTEBOOKLM_MCP_SETUP_GUIDE.md 的前期结论，目前需要手动获取想提问的notebooklm的ID，才能通过这个ID指定向哪个notebooklm提问。我们想自动化这个过程，内部同事在使用AI工具的时候，只需要输入想提问的内容，工具就能够自动获取到当前正在使用的notebooklm的ID，并且指定向这个notebooklm提问。
5. 由于notebooklm会隔一段时间就更换一次，这意味着它的ID会变。我们希望内部同事在使用这个工具的时候，不需要手动去更新ID。工具在内部同事的电脑上每次运行的时候，能够自动检测到当前正在生效的notebooklm的ID，并且指定向这个notebooklm提问。因此，需要提供一个办法，能够让我通过某种方式指定ID后，工具在内部同事的电脑上运行的时候，能够自动动态获取这个ID，并且后续的提问都向这个ID对应的notebooklm提问，并且当ID发生了变化，工具能够检测到并自动更新ID。这样一来，我就可以统一维护这个ID，更新ID之后，可以让所有使用工具的人的工具都自动更新ID。

以上只是我的大致思路，需要仔细想想实现方案。